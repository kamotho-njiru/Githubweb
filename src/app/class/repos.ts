export class Repos {
    constructor(public name: string, public watches_count:number, public forks_count:number, public stargazers_count:number,
        public description:string ) {     
        }
}
